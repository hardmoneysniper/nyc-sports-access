.env
.env.*
!.env.example
# Anchored to the repo root -- an unanchored "data/" also matched
# frontend/public/data/, silently excluding the geojson files the deployed
# app actually needs (caught before the first push, 2026-09-27).
/data/
*.pdf
node_modules/
__pycache__/
*.pyc
.venv/
venv/
.superpowers/
.claude/
*.log
*.log.crash*
